mixin contactform
  form#contactForm.mt-3(method="POST" action="/contact")
    .row
      .col-md-6.mb-3
        label.sr-only(for='name') Name
        input#name.form-control(name="name" type='text', placeholder='Name', value='', required='')
        .invalid-feedback
          | Valid first name is required.
      .col-md-6.mb-3
        label.sr-only(for='email') Email
        span.text-muted
        input#email.form-control(name="email" type='email', placeholder='your@example.com')
        .invalid-feedback
          | Please enter a valid email address for shipping updates.

      .col-md-12
        .form-group
          label.sr-only(for='body') Message
          textarea.mb-3#body.form-control(name="body" placeholder='Please, write your message here...', value='', required='')
          .invalid-feedback
            | Valid last name is required.
        .form-group
          label.sr-only(for='phone') Phone
          input#phone.form-control(name="phone" type='number', placeholder='015117367316' required)
          .invalid-feedback
            | Please enter a valid phone for shipping updates.
        
        label.sr-only(for='location') Location
        select.mb-3.custom-select.text-muted(name="location")
          option(selected="") Select your location here...
          each location in locations
            option(value=(location._id))=location.name
        
    
        p.dataPrivacyLink
          label.checkbox.TermsofService.text-muted
            | I have read and agree to the
            input(type='checkbox', name='TermsofService[]', value='I have read and agree to the', required='')
            span.checkmark
          a.ml-1(data-toggle='modal', data-target='#dataPrivacy')
            | Data
            | Privacy
        if companytour
          input(type="hidden" name='companytour' value=true)            
        .text-center
          button.btn.btn-lg.btn-gradient-secondary.m-auto(type="submit") SEND   
