mixin eventlist(public)
  .container.py-5
    h1.display-1 All events in our locations
    .row.my-5
      each location, locationname in locationEvents
        .col-md-6
          h2=locationname
          each event in location
            +singleEvent(event)
