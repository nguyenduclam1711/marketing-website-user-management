mixin ourCoursesSection
  section#services.section-services
    .container.container-courses
      .section-title-area
        h1.display-1
          | Our Courses
      .row
        each course in courses
          .col-md-6.col-lg-4
            .service-item
              a.service-link(href="/courses/"+course.slug)
                span.screen-reader-text
                  = course.title
              .service-image.icon-image.mb-5
                img.h-100(src=course.icon, alt=course.title)
              .service-content
                h4.service-title= course.headline
                p=course.title

      .section-button-wrap
        a.btn.btn-primary.btn-lg.active(href='/courses/', role='button', aria-pressed='true') Browse Courses
